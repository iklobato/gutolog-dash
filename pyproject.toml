[project]
name = "merge"
version = "0.1.0"
description = "Merge Excel freight data and Streamlit dashboard"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pandas>=2.0",
    "openpyxl>=3.1",
    "streamlit>=1.28",
]

[tool.uv]
